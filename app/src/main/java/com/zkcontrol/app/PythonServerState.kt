package com.zkcontrol.app

/**
 * حالة على مستوى العملية (Process) وليست مرتبطة بأي نسخة Activity واحدة.
 * كائنات Kotlin `object` تُهيَّأ مرة واحدة فقط لكل عملية تشغيل، فتبقى القيمة
 * صحيحة حتى لو أعاد أندرويد إنشاء MainActivity (تدوير الشاشة، العودة من
 * الخلفية، إلخ) بدون أن يقتل العملية بالكامل - وبالتالي نتجنب محاولة تشغيل
 * خادم Flask مرة ثانية على نفس المنفذ بينما الأول لا يزال يعمل.
 */
object PythonServerState {
    @Volatile
    var started = false
}
